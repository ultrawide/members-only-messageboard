extends layout

block content
  if user
    p Enter in the secret passcode to acquire membership status! In case you forgot, "Life is great!"
    p Admin priviledges can also be acquired this way, but the passcode is in the source code!

    form(method='POST', action='')
        div.form-group
        label(for="passcode") Passcode:
        input(type='text', name="passcode")
        button.btn.btn-primary(type='submit') Submit
  else
    p Please login to request membership
